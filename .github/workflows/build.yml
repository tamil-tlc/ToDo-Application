name: new one

on:
 push:
  branches:
   -main

jobs:
 build:
  runs-on: ubuntu

  steps:
   - name: checkout
     uses: actions/checkout@v3

   - name: set up java 
     uses: actions/setup-java@v3
     
      

  
